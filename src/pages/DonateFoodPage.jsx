import { UtensilsCrossed } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function DonateFoodPage() {
  return (
    <PlaceholderPage
      title="Donate Food"
      icon={UtensilsCrossed}
      message="The food listing form is being built — quantity, food type, expiry, photo upload, and a one-tap submit straight to nearby NGOs."
    />
  );
}
