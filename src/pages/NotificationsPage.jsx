import { Bell } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function NotificationsPage() {
  return (
    <PlaceholderPage
      title="Notifications"
      icon={Bell}
      message="Food alerts, claim confirmations, and delivery updates will show up here once the notifications API is connected."
    />
  );
}
